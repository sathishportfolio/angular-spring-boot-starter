# FAQ

* Why we need to set driver-class-name: com.mysql.cj.jdbc.Driver in application.yml
* Why we need to set dialect: org.hibernate.dialect.MySQLDialect in application.yml
* Formula for Choosing maximum-pool-size
* How will you enable batching for high-throughput inserts/updates